const { assert, expect } = require("chai");
const priceListFormatter = require("../index.js");

describe('PriceListFormatter problem', function () {
    it('priceListFormatter should be a function', function () { priceListFormatter(1) });
})